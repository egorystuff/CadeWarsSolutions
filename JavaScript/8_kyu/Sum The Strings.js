// Create a function that takes 2 integers in form of a string as an input, and outputs the sum (also as a string):

// Example: (Input1, Input2 -->Output)

let sumStr = (a, b) => String(Number(a) + Number(b));

let value = sumStr('4', '5');

console.log(value);
